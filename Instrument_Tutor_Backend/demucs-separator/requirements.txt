# Audio Separation with Demucs (GPU Accelerated)
# Works with CUDA-enabled GPUs (RTX 4050, etc.)

# Core dependencies
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0
demucs>=4.0.0

# Audio processing
numpy>=1.22.0
soundfile>=0.12.0

# Optional: Install PyTorch with CUDA support using:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
